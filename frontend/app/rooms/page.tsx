import { Header } from "@/components/layout/Header";
import { RoomCard } from "@/components/rooms/RoomCard";
import { getRooms } from "@/lib/api/client";
export default async function RoomsPage() { const rooms = await getRooms(); return <main><div className="relative bg-[#526b60] pb-16 pt-32 text-white"><Header /><div className="container"><p className="eyebrow text-[#f0a18a]">The rooms</p><h1 className="display mt-4 max-w-2xl text-7xl leading-[.9]">Stay somewhere<br /><em>with a pulse.</em></h1></div></div><section className="section-pad"><div className="container"><div className="mb-12 flex items-center justify-between border-b border-[var(--line)] pb-5"><p className="text-sm text-[var(--muted)]">{rooms.length} considered spaces</p><select className="border-0 bg-transparent text-sm font-semibold" defaultValue="featured"><option value="featured">Featured first</option><option value="price">Price: low to high</option></select></div><div className="grid gap-x-8 gap-y-12 md:grid-cols-3">{rooms.map((room, index) => <RoomCard key={room.id} room={room} index={index} />)}</div></div></section></main>; }