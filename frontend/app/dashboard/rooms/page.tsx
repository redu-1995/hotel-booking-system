import Link from "next/link";
export default function DashboardRooms() { return <DashboardList title="Rooms" detail="12 rooms · 9 available today" rows={["A01 · The Garden Room", "B04 · The Atelier Suite", "C12 · The Courtyard King"]} />; }
function DashboardList({ title, detail, rows }: { title: string; detail: string; rows: string[] }) { return <main className="min-h-screen bg-[#eef0ea] p-6 md:p-16"><Link href="/dashboard" className="text-sm text-[var(--muted)]">← Dashboard</Link><p className="eyebrow mt-16">Operations</p><h1 className="display mt-2 text-7xl">{title}</h1><p className="mt-2 text-[var(--muted)]">{detail}</p><div className="mt-10 max-w-3xl divide-y divide-[#d8ded5] border-y border-[#d8ded5] bg-white">{rows.map((row) => <div key={row} className="flex justify-between p-5"><span>{row}</span><span className="text-sm text-[var(--coral)]">View ↗</span></div>)}</div></main>; }