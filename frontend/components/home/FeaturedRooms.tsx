import Image from "next/image";
import Link from "next/link";
import type { HomeRoom } from "@/lib/home-data";

export function FeaturedRooms({ rooms }: { rooms: HomeRoom[] }) {
  return <section className="container py-20 text-center"><p className="eyebrow">Accommodation & comfort</p><h2 className="mt-3 font-serif text-4xl font-bold">Find your perfect room</h2><p className="mt-2 text-sm text-[#718092]">Choose from comfortable spaces designed for restful stays.</p><div className="mt-8 grid gap-5 text-left sm:grid-cols-2 lg:grid-cols-3">{rooms.map((room) => <article key={room.id} className="overflow-hidden rounded border border-[#e5e7e8] bg-white shadow-sm"><div className="relative h-48"><Image src={room.image} alt={room.name} fill sizes="(max-width: 768px) 100vw, 33vw" className="object-cover" /><span className="absolute left-2 top-2 bg-[#062c53] px-2 py-1 text-xs font-bold text-white">{room.tag}</span><span className="absolute bottom-2 right-2 rounded bg-white px-2 py-1 text-xs font-bold text-[#062c53]">{room.price} / night</span></div><div className="p-4"><h3 className="font-serif text-xl font-bold">{room.name}</h3><p className="mt-1 text-sm text-[#718092]">{room.description}</p><div className="mt-3 flex flex-wrap gap-3 border-t border-[#edf0f2] pt-3 text-xs text-[#637487]"><span>Up to {room.maxGuests} guests</span><span>{room.bedType}</span></div><div className="mt-4 flex gap-2"><Link href={`/rooms/${room.id}`} className="flex-1 border border-[#bfc9d2] py-2 text-center text-sm font-bold">View details</Link><Link href="/booking" className="flex-1 bg-[#062c53] py-2 text-center text-sm font-bold text-white">Book now</Link></div></div></article>)}</div><Link href="/rooms" className="mt-7 inline-block border border-[#bfc9d2] bg-white px-5 py-3 text-sm font-bold">Explore all rooms</Link></section>;
}
